// Kept as the bot's entry point; the rules live in hebrew.js, a copy of the
// site's module so both sides score identically.
export { normalize, tokenize, percentOf, sizesOf, credit } from './hebrew.js';
