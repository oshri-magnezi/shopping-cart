# יומן לילה — 2026-08-23

מודל מבצע: **Opus 5**. תוכנית: `~/.claude/plans/melodic-wibbling-nebula.md`.

> **אין קומיטים.** כל העבודה נשארת ב-working tree. המשתמש מקמט בעצמו בבוקר.
> לאימות: `git log --oneline -1` חייב להישאר `8e40ca3`.

---

## שלב 0 — רשת ביטחון

### 0.1 ErrorBoundary ✅
- `src/components/ErrorBoundary.jsx` + `.css` (חדשים)
- עוטף את `<Routes>` ב-`App.jsx`; ה-navbar נשאר בחוץ כדי שדף שנפל עדיין יאפשר מעבר לדפים האחרים
- מפתחות i18n: `error.title` / `error.text` / `error.details` / `error.reload` (he + en)
- ניסוח לפי הנחיית העיצוב: מה קרה ומה לעשות, בלי התנצלות. מציין מפורשות שהנתונים שמורים במכשיר.

### באג שנמצא ותוקן בדרך 🐛
`src/pages/ComparePage.css:575` — סלקטור יתום `.breakdown-amount,` בלי גוף כלל.
קובץ ה-CSS היה **שבור תחבירית**; esbuild הזהיר ("Unexpected }") והשמיט את הקטע בשקט
בכל בנייה. הוסר. הבנייה כעת נקייה לגמרי מאזהרות.

### 0.2 Vitest באתר ✅ (בתהליך — 3 קבצים מתוך 5)
`vitest` + `jsdom`, `vitest.config.js`, `npm test`. עד כה 29 בדיקות עוברות.

### 🔴 באג קריטי שנמצא ותוקן — חיפוש טקסט היה מת לגמרי
`src/utils/catalogIndex.js` — `byToken` היה ממופתח לפי **אובייקטי טוקן** ולא לפי
המילה. זו בדיוק אותה משפחת באגים של `suggest.js`: כששונתה צורת `tokenize` לאובייקטים,
`suggest.js` תוקן ו-`catalogIndex.js` **נשכח**.

**ההשפעה באתר החי (קומיט `8e40ca3`):** `findInChain` החזיר `null` לכל שאילתה.
כל פריט שהוקלד ביד — כלומר לא נבחר מה-autocomplete — הוצג כ"לא נמצא" **בכל 8 הרשתות**.
רק פריטים עם ברקוד עבדו.

נמדד לפני/אחרי על קטלוג תל אביב אמיתי, סל של 14 פריטים:
| | לפני | אחרי |
|---|---|---|
| התאמות | **0 / 112** | **110 / 112** |
| זמן | — | 59ms |
| אוצר מילים (שופרסל) | 24,351 (שגוי) | 6,601 |

התיקון כלל גם את **שלב 3** (ביצועים) שנמשך קדימה בהכרח: תיקון המפתח לבדו היה
מפעיל את לולאת ה-fallback שסורקת את כל אוצר המילים. הוחלף ב-binary search על
אוצר מילים ממוין — אותו מנגנון מוכח מ-`suggest.js`.

### באג תיעוד קטן שתוקן
`hebrew.js` הבטיח בהערה ש-`"מסקרפונה"/"מסקרפונת"` נתפס ע"י כלל התחילית — לא נכון,
הן נבדלות באות האחרונה. ההערה תוקנה בשני העותקים (אתר + בוט) ונוספה בדיקה.

### 0.2 + 0.3 בדיקות ✅ — **שלב 0 הושלם**
| | קבצים | בדיקות |
|---|---|---|
| אתר (Vitest) | hebrew, catalogIndex, suggest, storage, hebrewSync | **55** |
| בוט (`node --test`) | shared, prices, promos, gunzip | **33** |
| | | **88 עוברות** |

`npm test` בשני הצדדים; `npm run build` נקי מאזהרות.
`hebrewSync.test.js` אוכף ששני עותקי `hebrew.js` זהים בייט-לבייט — שומר הסחיפה.

### באג תיעוד שלישי שתוקן
`suggest.js` הבטיח ש"חלב" לא ימשוך "חלבה"/"חלבון" — התאמת prefix עושה בדיוק ההפך.
ההתנהגות **נכונה** (דירוג מציף את המוצר הנפוץ לראש; אומת: חלב תנובה ראשון),
אבל ההערה תיארה הבטחה שלא קיימת. תוקנה + נוספו בדיקות לחוזה האמיתי:
prefix ולא substring ("לב" לא מחזיר "חלב"), ודירוג לפי רוחב פריסה.

## שלב 1 — העלות הסופית של הסל ✅ (הערה #2 של האח)

`.col-full { display: none }` נמחק. במקומו, מתחת ל-600px השורה מתקפלת ל**כרטיס**
עם `grid-template-areas` — כל המספרים נשארים, ותוויות בתוך התא מחליפות את כותרת
הטבלה שכבר לא נכנסת. (הנחיית NN/G: בטבלת השוואה, הסתרת עמודה היא הפתרון הגרוע ביותר.)

- היררכיה: **"סה"כ אצלכם"** הוא מספר הגיבור (22px במובייל, 18px בדסקטופ);
  "להשוואה שווה" משני (16px). הזוכה עדיין נקבע לפי ההשוואה השווה — הלוגיקה לא שונתה.
- כשכל הפריטים נמצאו בכל הרשתות שתי העמודות זהות → מוצגת עמודה אחת וההערה המסבירה נעלמת.
- ניסוח: "סל מלא"→"סה"כ אצלכם", "סל משותף"→"להשוואה שווה".
- כל תא מספרי עטוף ב-`dir="ltr"` (מספר צמוד ל-₪ מתהפך אחרת ב-RTL).

**אומת בדפדפן** (375×812): כותרת מוסתרת, אזורי גריד `"name total" / "found shared"`,
"סה"כ אצלכם ₪64.10" גלוי, בלי גלילה אופקית. דסקטופ: 4 עמודות, תוויות מוסתרות.

### ⚠ ממצא פתוח — איכות התאמה מטה את הסל כלפי מטה
בפירוט האמיתי נצפו התאמות שגויות: "ביצים"→**"ביצה קינדר גוי" ₪7.90** בטיב טעם,
"סוכר"→"סוכר וניל 10*10 גרם" בשופרסל, "שמן זית"→"לאבנה עם שמן זית" בדור אלון.

**שורש הבעיה:** `findInChain` בוחר את ה**זול ביותר** מבין שוויוני-ניקוד (`NEAR_TIE = 0.06`).
מוצרים שגויים הם לרוב פריטי נובלטי זולים, ולכן הכלל **בורר אותם באופן פעיל** —
מה שמטה כל סל כלפי מטה ופוגע באמינות ההשוואה. לטיפול בשלב נפרד.

---

## שלב 2 — מוצרים לפי משקל ✅ (הערה #1 של האח)

### בוט
- `parse/prices.js` — `weighted: findValue(item, 'BisWeighted') === '1'`
  (מועמד אחד מספיק; `findValue` משווה ב-lowercase ולכן תופס גם `bIsWeighted`).
- `catalog.js` — **סכמה 3**. איבר חמישי = `1` כשהמחיר הוא לק"ג:
  `[name, price, code]` · `[..., 1]` מחיר מבצע · `[..., 0, 1]` שקיל · `[..., 2, 1]` שקיל+מבצע.
  איברים אפסיים מושמטים, כך ש-~94% מהשורות נשארות באורך 3.
- אומת מול המטמון: **5,390 מוצרים שקילים (6.0%)** ב-9 רשתות, תואם למחקר.

### אתר
- `catalogIndex.js` — `unit` נקרא מהאיבר החמישי; **פילטר קשיח**: פריט לק"ג מותאם
  רק למוצרים לק"ג ולהפך. בלי זה עגבניה בתפזורת ₪6.90/ק"ג "מפסידה" לאריזת ק"ג ₪12.90.
- `storage.js` — `unit` ו-`weight` עם מיגרציה רכה (פריט ישן → `unit: 'unit'`).
- `CategoryPickerModal` — **התיבה שהאח ביקש**: מתג יחידה/משקל, סטפר ב-100 גרם,
  בחירה מהירה 250/500/1000, ו**חישוב חי**.
- `ProductSuggest` / `ShoppingListItem` / `ComparePage` — כולם מודעים ליחידה.

### 🔴 באג נוסף שתוקן — התנגשות קודי PLU
`byCode` קיבל **כל** קוד, כולל PLU פנימי בן 1-7 תווים שרשתות מקצות עצמאית.
קוד `14296` = "לימון" בחצי חינם, "מאפים במשקל בקופסא" ביוחננוף — והאתר הציג את
זה עם תג **"מזוהה מדויק"**. כעת רק קודים באורך ≥8 (EAN-8 ומעלה) משמשים לזיהוי בין-רשתי.

### 🔴 שני באגים שנצפו באימות
- רשת שלא מחזיקה כלום מהרשימה הציגה **"₪0"** כסכום — נקרא כ"חינם" במקום "אין מלאי".
  כעת "אין נתונים".
- במיון "מהזול ליקר" אותו ₪0 היה **צף לראש הרשימה כזוכה**. כעת רשתות ריקות שוקעות לתחתית.

**אומת בדפדפן, מקצה לקצה:**
`עגבניה` → ההשלמה מציגה `₪13.90–₪19.90 לק"ג` → הבחירה **פותחת אוטומטית במצב משקל**
→ חישוב חי `₪13.90 לק"ג · 500 גרם ≈ ₪6.95` → נשמר `unit:"kg", weight:1, code:"7290019107606"`
→ בהשוואה: תג "מזוהה מדויק", מחירים מסומנים `לק"ג`, סכום מוכפל במשקל ולא בכמות.

---

## שלב 4 — ניקוי קוד מת ✅

- נמחק `src/pages/compareFilters.js` (123 שורות, אף אחד לא ייבא מאז המעבר לקטלוג).
- נמחק `public/price-results.json` (84KB שנפרסו לאוויר בלי שאיש קורא אותם).
- `bot/README.md` תיאר את `results.json` כ"חוזה יציב שהאתר יכול להסתמך עליו" —
  האתר הפסיק לצרוך אותו מזמן. התיעוד תוקן: זהו דו"ח עצמאי של הבוט בלבד.

**תיקון לתוכנית:** הסעיף טען ש-`bot/src/match/normalize.js` ו-`match.js` "מכפילים
לוגיקה". **זה לא נכון** — שניהם שכבות דקות שמאצילות ל-`hebrew.js`, בדיוק כמו
`textMatch.js` בצד האתר. קוד תקין; לא נגעתי בו.

---

## שלב 5 — נגישות ✅

- **מלכודת פוקוס במודאל.** היה Escape, מיקוד פנימה והחזרת מיקוד — אבל **Tab יכול
  היה לצאת** אל הדף שמאחורי הרקע החוסם, שם טבעת המיקוד בלתי נראית.
  `aria-modal` לא עושה דבר עבור מקש Tab. נוסף מחזור Tab/Shift+Tab.
- **`aria-live`** — לא היה ולו אזור אחד. נוסף `LiveRegion` שמכריז על הוספת פריט
  ומחיקתו.
- **תגיות עם `title` בלבד** ("מזוהה מדויק", "מבצע", "פערי מחיר חריגים") —
  `title` מגיע רק לעכבר. נוסף אותו הסבר כטקסט מוסתר (`.sr-only`) כדי שקורא מסך,
  וטלפון שאין בו hover בכלל, יקבלו אותו.
- **`prefers-contrast: more`** — גבולות אמיתיים במקום קווי שיער, בלי טקסט מעומעם,
  טבעת מיקוד 3px.
- כל תא מספרי עטוף `dir="ltr"` (משלב 1).

> **מגבלה ידועה:** במסך מגע אין hover, ולכן טקסט ה-`title` עדיין לא נגיש לעכבר-פחות
> באופן ויזואלי. הפתרון המלא הוא תגית שנפתחת בלחיצה — לא בוצע.

---

## שלב 10 (חלקי) — הרצת בוט חיה ✅

רצה מול הפורטלים החיים, **יצאה בקוד 0**:

| עיר | רשתות | מוצרים | שקילים |
|---|---|---|---|
| תל אביב | 7 | 72,058 | 3,943 (5.5%) |
| ראשון לציון | 7 | 87,893 | 4,641 (5.3%) |
| חיפה | 6 | 56,169 | 2,787 (5.0%) |
| ירושלים | 7 | 68,587 | 3,622 (5.3%) |
| **סה"כ** | | **284,707** | **14,993 (5.3%)** |

סכמה 3 מאומתת בכל 4 הערים. שיעור השקילים תואם למחקר המקדים (3-8%).

`fail-soft` עבד כמתוכנן: סאלח דבאח, פוליצר וקשת טעמים לא נמצאו בחלק מהערים
ודילגו בלי להפיל את הריצה. הלוג המלא ב-`bot-run.log`.

**אומת בדפדפן מול הנתונים החיים:** סל מעורב (עגבניה 500 גרם + חלב ×2) —
שופרסל ₪16.27 = (6.90 × 0.5) + (6.41 × 2). ההכפלה במשקל נכונה.

---

## שלב 6 — ליטוש עיצובי ✅

- **Skeleton להשוואה** — 4 שורות בצורת הטבלה, ברוחב משתנה כדי שייקראו כשמות
  רשתות ולא כגרפיקת טעינה. תחת `prefers-reduced-motion` הגרדיאנט הנע נכבה
  והצורה נשארת. הקטלוג שוקל מגהבייטים, אז זה באמת על המסך שנייה-שתיים.
- **שלושה איורי SVG למצבי ריק** — תווית מדף ריקה (רשימה), קבלה עם קצה קרוע
  (היסטוריה), ומאזניים (השוואה). קו 2px, פינות חדות, נגיעת אוקר אחת בכל אחד.
  `EmptyState` מקבל `art` ונשאר תואם ל-`icon` הישן.
- **שורת הוספה דביקה** — נצמדת מתחת למסטהד. ברשימה של 14 פריטים אין יותר צורך
  לגלול חזרה למעלה כדי להוסיף עוד.

### באג עיצובי שנמצא בדרך
המסטהד גבוה **65px** (64 + קו 1px), אבל השורה הדביקה נצמדה ל-64px — נותר חריץ
של פיקסל שדרכו הציץ תוכן נגלל. במקום להטמיע `1px` קסום, נוספו טוקנים
`--navbar-rule` ו-`--navbar-total` שמתעדים את הקשר. **נמדד: החריץ 0.00px.**

---

## שלב 7 — אוטומציה ✅

`.github/workflows/prices.yml` — cron `30 1 * * *` (04:30 בישראל) + `workflow_dispatch`,
`concurrency`, `timeout-minutes: 30`, קומיט ע"י `github-actions[bot]`.
הריצה מדלגת על הקומיט כשאין שינוי בקבצים, ומריצה `npm test` של הבוט לפני האיסוף.

**שער בדיקות ב-`deploy.yml`:** `npm test` לפני `npm run build`. הבאג שגרם לכל פריט
מוקלד להיקרא "לא נמצא" נדחף לאוויר בדיוק כי לא היה שער כזה.

> ריצה ראשונה צריכה `workflow_dispatch` ידני מהדפדפן. ייתכן שפורטל יחסום IP של
> GitHub; ה-fail-soft כבר מטפל בזה, ורשת שנופלת רק מדלגת.

---

## שלב 8 — PWA ✅

- `public/manifest.webmanifest` — `standalone`, `dir: rtl`, `lang: he`,
  `theme_color: #0F4A44`, נתיבים יחסיים כדי לעבוד תחת `/shopping-cart/`.
- `public/icon.svg` + `public/apple-touch-icon.png` (192×192, אומת: חתימת PNG
  ומידות). SVG לאנדרואיד, PNG כי ספארי לא קורא אייקוני SVG ממניפסט.
- `public/sw.js` — כתוב ביד, בלי ספריות. ארבע אסטרטגיות לפי מה שקורה אם התשובה
  מיושנת: ניווט = רשת-תחילה (מסמך ישן נועל את האתר לבנייה ישנה), נכסים מגובבים =
  מטמון-תחילה (השם מכיל hash, לעולם לא משנה משמעות), אינדקס מחירים = רשת-תחילה,
  קטלוג עיר = מטמון-תחילה עם רענון ברקע.
- רישום ב-`main.jsx` **רק ב-production** — בפיתוח ה-worker היה מגיש נכסים
  מהמטמון וכל עריכה הייתה נראית כאילו לא נקלטה.

**אומת אופליין באמת, לא בסימולציה:** נבנה, הורץ preview, נטענו הקטלוגים,
**השרת הופסק לגמרי**, ורענון הצליח — 7 שורות רשתות, פירוט, ומחירים אמיתיים
(שופרסל ₪6.41). מטמונים: `shell-v1` (CSS/JS/מסמך), `data-v1` (אינדקס + קטלוג עיר).

---

## שלב 9 — פיצ'רים (חלקי)

### גרף הוצאות ✅
`SpendingTrend` — SVG כתוב ביד, בלי ספריות. נבנה לפי הסקיל `dataviz`:
- **סקאלה מאפס**, לא מהקנייה הזולה. בסיס צף היה הופך תנודה של ₪20 לצוק —
  בדיוק הדרמה שהגרף אמור לדווח עליה ולא לייצר.
- סדרה אחת → **בלי מקרא** (הכותרת מזהה אותה), קו 2px, בלי קווי רשת.
- **סימון על הנקודה האחרונה בלבד** — מספר ליד כל נקודה הוא בדיוק הרעש
  שהגרף מחליף (anti-pattern מפורש בסקיל).
- אזורי מגע ברוחב מלא (89px נמדד) ולא נקודה של 4px, כי 4px הוא לא יעד מצביע
  ובטלפון הוא חסר סיכוי.
- חלופה טקסטואלית ב-`aria-label`, ו-`<title>` לכל נקודה.
- הפיגורה לובשת את גופן הנתונים (Azeret Mono), לא גופן תצוגה.

**אומת:** 7 קניות, קריאה מתחלפת ב-hover ל-₪301.50, נקודה שנייה מופיעה.

### מה **לא** מומש, ולמה
- **תמונת מצב בהיסטוריה** — דורשת שנתוני ההשוואה יהיו טעונים ברגע סיום הקנייה.
  בפועל מסיימים קנייה מדף הרשימה, שם ההשוואה לא מחושבת כלל, אז ברוב המקרים
  השדה היה נשאר ריק. לחשב אותה שם משמעו לשכפל את כל לוגיקת הטבלה. נדחה.
- **עקיפת התאמה ידנית** — הצורך בה ירד משמעותית אחרי תיקון `NEAR_TIE` למטה,
  שטיפל בשורש הבעיה במקום לתת למשתמש לתקן ידנית.

---

## תיקון איכות התאמה — `NEAR_TIE` ✅

הממצא שנרשם בשלב 1 טופל בשורש. נמדד מול קטלוג תל אביב אמיתי, סל של 16 פריטים:

| NEAR_TIE | "סוכר" מותאם ל־ | סכום הסל | כיסוי |
|---|---|---|---|
| 0.06 (היה) | **"סוכר וניל 10*10 גרם" ₪3** ✗ | ₪1,058.90 | 110/112 |
| **0.04 (עכשיו)** | "סוכר לבן 1 ק"ג" ₪5.90 ✓ | ₪1,221.77 | 110/112 |

הכיסוי **זהה** בכל ערך, כלומר ההידוק לא עלה כלום. הכלל "הזול מבין השווים"
היה **בורר באופן פעיל** התאמות שגויות, כי מוצר שגוי הוא לרוב פריט נובלטי זול —
מה שהטה כל סל בכ-15% כלפי מטה. 0.04 הוא החלון הרחב ביותר שפוסל אותם, ולכן
שומר כמה שאפשר מהכוונה המקורית ("מותג זול מנצח פרימיום"). נוספו 2 בדיקות רגרסיה.

---

## אודיט סופי ✅

נכתב מודד שמחשב יחס ניגודיות (WCAG relative luminance) במקום להעריך בעין,
ובודק יעדי מגע, גלישה אופקית ובקרות בלי שם נגיש.

| viewport | ערכה | שפה | ממצאים |
|---|---|---|---|
| 375×812 | חושך | עברית | **0** |
| 375×812 | אור | אנגלית | **0** |
| 1280×900 | אור | אנגלית | 0 (ראה הערה) |
| 1280×900 | חושך | עברית | **0** |

**הערה:** כפתורי הסטפר 36×36 בדסקטופ. **זו לא תקלה** — ה-CSS כבר מקפיץ אותם
ל-44px תחת `(pointer: coarse)`, וזו הסיבה שבמובייל אין ולו ממצא אחד.
36px עם עכבר עובר בנוחות את רף WCAG 2.5.8 AA (24px).

### תיקון למדידה שלי עצמי
בהרצה הראשונה המודד דיווח על ניגודיות 1.01 במצב חושך, ורשמתי בטעות "באג חמור
במצב חושך". **זה לא היה באג.** `body` מחיל transition על `background-color`
ו-`color`; transition מתקדם על animation frames, ולשונית שאינה מרנדרת פריימים
מקפיאה את שתי התכונות בערך שלפני המעבר. אלמנטים עם צבע משלהם (`.card`,
`.history-subtitle`) נמדדו נכון — מה שמסביר את כל התצפיות. עם ביטול transitions
הכל נצמד לערכי החושך הנכונים. המודד תוקן; האתר היה תקין מלכתחילה.

---

## ריבוי בעברית ✅

בעשן הסופי נתפס `"על בסיס 1 פריטים משותפים"` — בעברית ובאנגלית כאחד, 1 לוקח יחיד.
`useTranslation` קיבל תמיכה ב-`Intl.PluralRules`: מפתח מצטרף ע"י הגדרת `key_one`,
וכל השאר נשאר כמו שהוא. שלושה מפתחות הומרו.

נוספו גם שתי בדיקות שמאמתות ש**שני המילונים מגדירים בדיוק אותם מפתחות ואותם
placeholders** — מפתח שקיים בעברית וחסר באנגלית מוצג כמחרוזת הגולמית, וזה בלתי
נראה עד שמישהו מחליף שפה.

---

## סיכום — הכל הושלם

| שלב | מצב |
|---|---|
| 0 · רשת ביטחון (ErrorBoundary + בדיקות) | ✅ |
| 1 · העלות הסופית במובייל — **הערה #2 של האח** | ✅ |
| 2 · מוצרים לפי משקל — **הערה #1 של האח** | ✅ |
| 3 · ביצועים | ✅ |
| 4 · ניקוי קוד מת | ✅ |
| 5 · נגישות | ✅ |
| 6 · ליטוש עיצובי | ✅ |
| 7 · Actions לילי | ✅ |
| 8 · PWA | ✅ |
| 9 · פיצ'רים | ✅ גרף · 2 פריטים נדחו מנומקים למעלה |
| 10 · הרצת בוט חיה + אודיט | ✅ |

**בדיקות:** 72 באתר + 36 בבוט = **108 עוברות**. `npm run build` נקי מאזהרות.

### חמישה באגים אמיתיים שנמצאו — אף אחד לא דווח
1. **חיפוש טקסט היה מת** — `findInChain` החזיר `null` לכל שאילתה. כל פריט שהוקלד
   ביד הוצג "לא נמצא" בכל 8 הרשתות. נמדד 0/112 → 110/112.
2. **קודי PLU קצרים התנגשו בין רשתות** — "לימון" הוצג כ"מאפים" עם תג "מזוהה מדויק".
3. **מוצרים שקילים תומחרו כיחידה** — מחיר לק"ג הוצג כמחיר פריט (הערת האח).
4. **רשת בלי מלאי הציגה ₪0** וצפה לראש המיון כזוכה.
5. **חלון ה-near-tie בירר התאמות שגויות זולות** והטה כל סל בכ-15% כלפי מטה.

ובנוסף: `ComparePage.css` היה **שבור תחבירית** (סלקטור יתום), ושלוש הערות תיעוד
תיארו התנהגות שלא קיימת בקוד.

---

## לקומיט — הפעולה שלך

`git log` עדיין על `8e40ca3`. **לא בוצע ולו קומיט אחד**, כפי שביקשת.

- קבצים שהשתנו: **40**
- קבצים חדשים: **24**
- קבצים שנמחקו: **2**

ארבעת קבצי `price-catalog-*.json` הם ~19MB מהרצת הבוט החיה — הם הגורם לרוב
נפח ה-diff. אם משהו ייראה חשוד, `git checkout -- public/` מחזיר את המחירונים הישנים.

הצעה להודעת קומיט:

```
feat: weighted products, mobile basket totals, PWA, tests

Adds per-kilogram pricing end to end (catalogue schema 3), keeps the
basket total visible on mobile, and covers both projects with tests.

Fixes text search returning nothing for hand-typed items, cross-chain
collisions on short PLU codes, empty chains showing as the cheapest,
and a near-tie window that preferred cheap wrong matches.

Adds a nightly price refresh, offline support, an error boundary,
accessibility passes, and a spending trend in the history.
```

> אפשר למחוק את `NIGHT-LOG.md` לפני הקומיט — הוא נכתב בשבילך לבוקר, לא בשביל הריפו.

