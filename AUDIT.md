# AUDIT

יומן ביקורת אוטומטי. סעיף חדש בראש, ישנים נשמרים — הערך הוא במגמה בין הרצות.

---

## 2026-09-11 · 17:05 — הרצה שלישית

**מצב: האתר עצמו תקין ומשתפר — תקלת חלב-הקוקוס מההרצה הקודמת תוקנה — אבל צינור
הרענון הלילי שבור לגמרי: 19 מתוך 19 הרצות מתוזמנות נכשלו ברצף מאז שה-workflow
קם, והקטלוג לא התרענן ולו פעם אחת מאז 23 באוגוסט.** זה הממצא המרכזי של ההרצה
הזו וממנו נובע הכול: כל שאר המדדים (בדיקות, בנייה, התאמות, נגישות) נבדקים מול
אותו קטלוג בן 19.5 ימים כמו ההרצה הקודמת, ולכן זהים לו כמעט בביט לביט.

| מדד | ערך | הרצה קודמת |
|---|---|---|
| בדיקות אתר | 94 עוברות* | 93 עוברות (+1) |
| בדיקות בוט (מקומי, Windows) | 36 עוברות | 36 עוברות (ללא שינוי) |
| בנייה | נקייה, ללא אזהרות | נקייה |
| JS (gzip) | 80.99 kB | 81.00 kB (-0.01 kB) |
| CSS (gzip) | 8.62 kB | 8.60 kB (+0.02 kB) |
| סכמת קטלוג | 3 | 3 |
| `generatedAt` | 2026-08-23 02:14 UTC — **467.8 שעות / 19.5 ימים** | אותו `generatedAt`, 90.6 שעות בזמן ההרצה הקודמת |
| Actions "Refresh prices" | **19/19 הרצות מתוזמנות נכשלו** | לא נבדק דרך ה-API בהרצה הקודמת |
| נגישות | 0 ממצאים אמיתיים (ראו כיסוי חלקי למטה) | 0 ממצאים |
| סל בקרה (שופרסל ת"א) | ₪114.85 — זהה לחלוטין | ₪114.85 |

\* בהרצה הראשונה של `npm test` נכשל טסט אחד (`hebrewSync.test.js`, ראו למטה);
בשתי הרצות חוזרות מיידיות הוא עבר. נספר כ-94 עוברות כי זו התוצאה היציבה.

### ממצא מרכזי: ה-workflow הלילי נכשל בכל הרצה מאז שהוקם — הקטלוג לא התרענן אף פעם

בהרצה הקודמת ציינתי ש-`generatedAt` לא זז ושזה כנראה רק local pull ישן. הפעם
בדקתי את זה בפועל ולא זו הסיבה:

- **`git fetch origin main` → 0 קומיטים קדימה, 0 אחורה.** ה-`main` המקומי זהה
  ל-`origin/main` בדיוק. אין קומיט חדש יותר על השרת שפשוט לא נמשך.
- **מול ה-API של GitHub Actions**, ל-workflow `Refresh prices`
  (`.github/workflows/prices.yml`, רץ כל לילה ב-01:30 UTC) יש 19 הרצות מתוזמנות
  עד כה — **כל ה-19 הסתיימו ב-`failure`**, ברצף יומי מלא מ-24 באוגוסט ועד היום
  (11 בספטמבר). זו לא נפילה מזדמנת; ה-pipeline מעולם לא הצליח פעם אחת מאז
  שהוקם.
- כל ההרצות נכשלות **באותו שלב בדיוק**: `Check the bot still parses correctly`
  (כלומר `npm test` בתוך `bot/`, עם `npm ci` נקי). השלבים שאחריו —
  `Build the catalogues` ו-`Commit the refreshed catalogues` — מדווחים
  `skipped` בכל פעם. גם אם כל השאר בסדר, שום קטלוג לא יגיע ל-`main` כל עוד
  השלב הזה נכשל.
- ניסיתי לשחזר מקומית באופן שמדמה את ה-CI במדויק: `git worktree` מנותק על אותו
  קומיט (`7431df3`, הקומיט האחרון על `main`), ואז `npm ci` + `npm test` בתוך
  `bot/` — **עבר נקי, 36/36**, גם בעותק העבודה הרגיל וגם בעותק הנקי הזה. כלומר
  זו לא בעיה שנראית ב-Node/Windows המקומי; היא ספציפית לריצה על ה-runner
  (`ubuntu-latest`).
- לא הצלחתי למשוך את הלוג המלא של השלב הכושל: גם `/actions/runs/{id}/logs`
  וגם `/actions/jobs/{id}/logs` החזירו `403 Must have admin rights to
  Repository` — ה-API הציבורי לא חושף לוגים של ריצות, גם ברפו ציבורי. מה
  שכן חשף את עצמו הוא ה-annotation האוטומטי של GitHub על ההרצה האחרונה:
  **"Node.js 20 is deprecated. actions/checkout@v4, actions/setup-node@v4 …
  are being forced to run on Node.js 24"**. ה-workflow נועל `node-version: 20`
  ב-`setup-node@v4`, אבל אם ה-runner כבר לא מכבד את זה ומריץ הכול על Node 24,
  זה מועמד סביר מאוד לתקלה — שינוי גרסת Node יכול לשבור תלות או התנהגות ב-
  `node --test` שלא רואים על Node 20/22 מקומי. זו השערה מבוססת, לא אישור
  סופי; צריך גישת admin לרפו כדי לראות את הפלט המדויק של הצעד.

**למה זה חשוב לקונה בפועל:** המחירים שמוצגים היום באתר הם מ-19.5 ימים אחורה,
בכל 4 הערים ובכל הרשתות. אם מחיר כלשהו זז אצל אחת הרשתות בחלון הזה, האתר
מציג אותו שגוי בלי שום סימן חיצוני לכך — אין "התיישן" גלוי בממשק. זה חמור יותר
מכל ממצא נגישות שנמצא (ולא נמצא) בהרצה הזו.

### קטלוג

| עיר | רשתות | מוצרים | שקילים |
|---|---|---|---|
| תל אביב | 7 | 72,058 | 5.5% |
| ראשון לציון | 7 | 87,893 | 5.3% |
| חיפה | 6 | 56,169 | 5.0% |
| ירושלים | 7 | 68,587 | 5.3% |

זהה בביט לביט להרצה הקודמת — צפוי, ראו למעלה: זה אותו קובץ קטלוג בדיוק
(`generatedAt` זהה), כי אף הרצת רענון לא הצליחה בינתיים.

פירוט לפי רשת ועיר (שקילים מתוך סה"כ המוצרים של אותה רשת), גם הוא זהה להרצה
הקודמת:

| רשת | ת"א | ראשל"צ | חיפה | י-ם |
|---|---|---|---|---|
| שופרסל | 7.7% | 6.3% | 6.1% | 7.2% |
| חצי חינם | 5.5% | **2.2%** ⚠ | 5.5% | 5.5% |
| רמי לוי | 4.7% | 4.3% | 3.9% | 3.3% |
| יוחננוף | 7.7% | 8.2%* | — | 6.7% |
| טיב טעם | 5.3% | 4.8% | 5.4% | 5.6% |
| קשת טעמים | — | 7.9% | — | — |
| דור אלון | 0.0%† | — | 0.0%† | 0.0%† |
| פרש מרקט | 3.4% | 3.2% | 5.0% | 4.6% |

⚠ **חצי חינם בראשון לציון עדיין ב-2.2%**, מתחת לטווח הצפוי (3-8%) — זהה
להרצה הקודמת, כצפוי מקטלוג שלא זז. עדיין נראה כמו סטייה ספציפית לקובץ/סניף
של ראשון לציון (בשלוש הערים האחרות אותה רשת בדיוק על 5.5%), לא רגרסיה גורפת.
יישאר סימן שאלה פתוח עד שיגיע קטלוג מרוענן אמיתי לבדוק אותו.

*יוחננוף בראשון לציון עדיין ב-8.2%, אותה חריגה זניחה מההרצה הקודמת.

†דור אלון ב-0.0% — יציב, לא תקלה (רשת נוחות, לא מוכרת בתפזורת).

### סל בקרה (שופרסל, תל אביב) — ותיקון ממצא קודם

```
חלב        -> חלב 3% מהדרין שקית 1 ל      ₪6.41
לחם אחיד   -> לחם אחיד 750 גרם            ₪7.36
ביצים      -> ביצים ארוזות XL 12יחידות    ₪15.48
שמן זית    -> שמן זית אורגני 750 מ"ל      ₪52.90
קוטג       -> קוטג מהדרין 9% 250 גרם      ₪6.40
אורז       -> אורז לסושי 1 ק"ג            ₪15.90
סוכר       -> סוכר לבן 1 ק"ג              ₪5.90
במבה       -> במבה מתוקה 30 גרם           ₪4.50
                                    סה"כ  ₪114.85
```

זהה לחלוטין להרצה הקודמת (אותו קטלוג, ראו למעלה). נבדק שוב דרך `findInChain`
האמיתי מ-`src/utils/catalogIndex.js`, לא הפונקציה הפשוטה של `textMatch.js`.

**תיקון אומת: "חלב" בפרש מרקט (ת"א) כבר לא נופל על חלב קוקוס.** בהרצה הקודמת
"חלב" נפתר שם ל"חלב קוקוס 400 מ"ל" (₪9.90). בדקתי את אותה שאילתה מול אותו
קובץ פרש מרקט ישירות — עכשיו היא נפתרת ל**"חלב טרי 3% תנובה 1.5" (₪11.02)**,
חלב פרה אמיתי. הסיבה נראית ב-`git diff` של `src/utils/hebrew.js` (וגם ב-
`bot/src/match/hebrew.js` הזהה לו): נוסף `KIND_WORDS` — רשימת מילים
("קוקוס", "סויה", "שקדים", "מיץ", "רסק", "קינדר" ועוד) שפוסלות התאמה כשהן
מופיעות **בכל מקום בשם המוצר**, לא רק במילה הראשונה כמו `CATEGORY_WORDS`
הישן. זה בדיוק התיקון שההרצה הקודמת המליצה עליו.

הרחבתי שוב את הבדיקה לכל 7 הרשתות בתל אביב (אותו סל בסיס) כדי לוודא שאין
תקלת-אחות במקום אחר: 22/24 שאילתות "חלב/ביצים/סוכר וכו'" בכל שבע הרשתות
נפתרות למוצר סביר; שתי ה"NO MATCH" היחידות (לחם אחיד ושמן זית בדור אלון) הן
ציפי — דור אלון היא רשת נוחות קטנה (1,562 מוצרים) שלא סביר שמוכרת אף אחד
מהשניים. לא נמצאה שום התאמה שגויה נוספת מהסוג שנמצא בהרצה הקודמת.

### נגישות

נבדקו `#/`, `#/compare`, `#/history`. **כיסוי חלקי יותר מההרצה הקודמת מטעמי
זמן:** ערכת בהיר+עברית ו-ערכת כהה+אנגלית (שני הצירופים ה"טבעיים"), כל אחת
בדסקטופ (1280) ורוב הבדיקות גם בנייד (375, מצביע גס). **לא נבדקו** הצירופים
הצולבים (כהה+עברית, בהיר+אנגלית) — פער מול ההרצה הקודמת שכיסתה את כל 2×2.
**בכל מה שכן נבדק: אפס כשלי ניגודיות, אפס יעדי-מגע מתחת ל-24px, אפס גלישה
אופקית, אפס פקדים בלי שם נגיש.**

שתי תקלות-מדידה ידועות מההרצה הקודמת נבדקו ואומתו כלא-רלוונטיות גם הפעם:

1. **טאב לא-גלוי (`document.hidden === true`) קפוא לאורך כל ההרצה**, בדיוק
   כמו בהרצה הקודמת (ניסיון שני להריץ שרת דרך `preview_start` נדחה שוב:
   "nobody is present to approve" בסשן לא-מוקצה; השרת הופעל ידנית דרך Bash
   כרקע). כתוצאה מכך `AnimatedCurrency` בעמודי ההשוואה וההיסטוריה הציג
   "‏0 ‏₪" באופן ויזואלי. לא נסמכתי על התצוגה הזו — הסכומים האמיתיים אומתו
   בנפרד דרך `findInChain` (ראו סעיף סל הבקרה למעלה).
2. **כפתורי עריכה/מחיקה של `SwipeRow`** עדיין נחתכים ב-`overflow:hidden` על
   `.swipe-row` וגלויים רק אחרי מחוות החלקה — סוננו מתוך תוצאות הניגודיות (6
   רכיבים ב-1.00–1.14, כולם בתוך `.swipe-actions`) מאותה סיבה שתועדה בהרצה
   הקודמת.

עמוד ההיסטוריה נטען הפעם בלי קריסה (הוזנו נתוני בדיקה תקינים עם
`completedAt`/`totalCost`/`itemCount`) — לא נבדק מחדש אם הפער ההגנתי שתועד
בהרצה הקודמת (`loadData()` לא מנקה רשומות `history` פגומות) עדיין קיים בקוד;
לא היה שינוי רלוונטי נראה ב-`storage.js`.

### בדיקות: כשל חד-פעמי לא-משוחזר ב-`hebrewSync.test.js`

בהרצה הראשונה של `npm test` בשורש, `hebrewSync.test.js` (בודק ש-
`src/utils/hebrew.js` ו-`bot/src/match/hebrew.js` זהים בייט לבייט) נכשל:
`expected false to be true`. בדקתי מיד עם `cmp`/`diff` על שני הקבצים בדיסק —
**הם זהים לחלוטין**, ואותו טסט עבר נקי בשתי הרצות חוזרות מיד אחר כך (גם
בבידוד וגם כחלק מהסוויטה המלאה, 94/94). הכי סביר: מירוץ חד-פעמי מול תהליך
עריכה/שמירה שרץ באותו רגע על אחד הקבצים (יש שינויים לא-מחויבים רבים בעץ
העבודה כרגע, כצפוי וכמצוין בהנחיות ההרצה). לא ממצא אמיתי — מצוין לתיעוד בלבד,
כדי שהרצה הבאה תדע שזה כבר נצפה ונבדק.

### קונסולה

ללא שגיאות אמיתיות בשום עמוד/ערכת-נושא/שפה/רוחב-מסך שנבדק — רק אזהרות
React Router הצפויות ולוגי HMR של Vite, זהה להרצה הקודמת.

### מה לא נבדק ולמה

- **לוג מלא של השלב הכושל ב-CI** (`Check the bot still parses correctly`) —
  שתי נקודות הקצה הרלוונטיות של ה-API (`/actions/runs/{id}/logs` ו-
  `/actions/jobs/{id}/logs`) מחזירות `403 Must have admin rights to
  Repository` גם ברפו ציבורי. יש רק annotation כללי ("exit code 1") ורמז
  עקיף (אזהרת Node 20→24). מי שיש לו הרשאת admin לרפו יכול לפתוח את
  https://github.com/oshri-magnezi/shopping-cart/actions/runs/34570269754/job/103170689658
  ולראות את הפלט המדויק.
- **לא הורץ מבחן ה-140 שאילתות המתויגות** (מההרצה הראשונה) — נשאר גם הפעם
  פער מוכר, הוחלף שוב בסל הבקרה המורחב על 7 הרשתות.
- **נגישות לא נבדקה בכל 4 צירופי ערכת-נושא/שפה** — רק שני הצירופים ה"טבעיים"
  (בהיר+עברית, כהה+אנגלית), לא כהה+עברית או בהיר+אנגלית. שווה להשלים בהרצה
  הבאה.
- **שרת הפיתוח שוב לא הופעל דרך `preview_start`** — נדחה מאותה סיבה
  (סשן לא-מוקצה). הופעל ידנית דרך Bash כרקע והצליח; זו הסיבה גם לתקלת ה-rAF
  שתועדה למעלה.

## 2026-08-27 · 00:05 — הרצה שנייה

**מצב: תקין ברובו.** שתי תקלות אמיתיות: התאמה שגויה של "חלב" לחלב קוקוס בפרש מרקט,
ורשת אחת ביריד אחד יוצאת מטווח השקילים הצפוי. שאר הבדיקות נקיות. שווה לשים לב:
הקטלוג לא התרענן מאז ההרצה הקודמת (אותו `generatedAt`), כך שכל מספרי הקטלוג
וסל הבקרה זהים ל-100% — זו לא תקלה, זה פשוט אין נתונים חדשים להשוות.

| מדד | ערך | הרצה קודמת |
|---|---|---|
| בדיקות אתר | **93 עוברות** | 79 עוברות (+14) |
| בדיקות בוט | 36 עוברות | 36 עוברות (ללא שינוי) |
| בנייה | נקייה, ללא אזהרות | נקייה |
| JS (gzip) | **81.00 kB** | 77.12 kB (+3.88 kB) |
| CSS (gzip) | **8.60 kB** | 6.53 kB (+2.07 kB) |
| סכמת קטלוג | 3 | 3 |
| `generatedAt` | 2026-08-23 02:14 UTC — **90.6 שעות** | 2026-08-23, 7.0 שעות בזמן ההרצה |
| נגישות | 0 ממצאים אמיתיים ב-3 עמודים × 2 ערכות נושא × 2 שפות | 0 ממצאים ב-3 עמודים |
| סל בקרה (שופרסל ת"א) | ₪114.85 — זהה לחלוטין | ₪114.85 |

### קטלוג

| עיר | רשתות | מוצרים | שקילים |
|---|---|---|---|
| תל אביב | 7 | 72,058 | 5.5% |
| ראשון לציון | 7 | 87,893 | 5.3% |
| חיפה | 6 | 56,169 | 5.0% |
| ירושלים | 7 | 68,587 | 5.3% |

זהים בביט לביט להרצה הקודמת — צפוי, כי `generatedAt` באינדקס (`2026-08-23T02:14:35Z`)
לא זז מההרצה הקודמת. **90.6 שעות** מאז אותה חתימה, מעל סף ה-48 שעות — אבל זה קורא
נגד עותק עבודה מקומי, לא נגד ה-workflow עצמו: אין כאן שום סימן שה-Action הלילי לא
רץ בשרת; פשוט לא בוצע `git pull` בעותק הזה מאז ה-23 באוגוסט. שווה לרענן את הענף
לפני ההרצה הבאה כדי שהביקורת תבדוק קטלוג עדכני.

פירוט לפי רשת ועיר (שקילים מתוך סה"כ המוצרים של אותה רשת):

| רשת | ת"א | ראשל"צ | חיפה | י-ם |
|---|---|---|---|---|
| שופרסל | 7.7% | 6.3% | 6.1% | 7.2% |
| חצי חינם | 5.5% | **2.2%** ⚠ | 5.5% | 5.5% |
| רמי לוי | 4.7% | 4.3% | 3.9% | 3.3% |
| יוחננוף | 7.7% | 8.2%* | — | 6.7% |
| טיב טעם | 5.3% | 4.8% | 5.4% | 5.6% |
| קשת טעמים | — | 7.9% | — | — |
| דור אלון | 0.0%† | — | 0.0%† | 0.0%† |
| פרש מרקט | 3.4% | 3.2% | 5.0% | 4.6% |

⚠ **חצי חינם בראשון לציון: 2.2%, מתחת לטווח הצפוי (3-8%).** זו הרשת היחידה שיצאה
מהטווח כלפי מטה בהרצה הזו (12,365 מוצרים, 270 שקילים בלבד). בשלוש הערים האחרות
אותה רשת בדיוק עומדת על 5.5% — כלומר זו סטייה ספציפית לסניף/קובץ של ראשון לציון,
לא רגרסיה גורפת בחילוץ `BisWeighted`. כדאי לעקוב: אם המספר הזה יישאר נמוך גם
בהרצה הבאה (עם קטלוג מרוענן), זה מצדיק בדיקה בקובץ המקור של הרשת לעיר הזו.

*יוחננוף בראשון לציון: 8.2%, מעט מעל תקרת ה-8%. סטייה של 0.2 נקודת אחוז בלבד —
ברמת רעש, לא ממצא בפני עצמו, אבל מוזכר כי זה חוצה את הסף שנקבע.

†דור אלון ב-0.0% בשלוש הערים: לא תקלה (רשת נוחות, לא מוכרת בתפזורת), כפי שצוין
בהרצה הקודמת. נשאר יציב.

### סל בקרה (שופרסל, תל אביב)

```
חלב        -> חלב 3% מהדרין שקית 1 ל      ₪6.41
לחם אחיד   -> לחם אחיד 750 גרם            ₪7.36
ביצים      -> ביצים ארוזות XL 12יחידות    ₪15.48
שמן זית    -> שמן זית אורגני 750 מ"ל      ₪52.90
קוטג       -> קוטג מהדרין 9% 250 גרם      ₪6.40
אורז       -> אורז לסושי 1 ק"ג            ₪15.90
סוכר       -> סוכר לבן 1 ק"ג              ₪5.90
במבה       -> במבה מתוקה 30 גרם           ₪4.50
                                    סה"כ  ₪114.85
```

זהה ל-100% להרצה הקודמת — לא רק הסכום, כל שורה. עקבי עם הקטלוג הבלתי-משתנה
(ראו לעיל). נבדק דרך `findInChain` האמיתי (לא `findBestMatch` הפשוט של
`textMatch.js`, שלא בשימוש באתר) — כולל כלל "הזול מבין הצמודים" (`NEAR_TIE=0.04`).

הרחבתי את הבדיקה לכל 7 הרשתות בתל אביב (סל זהה, לא ה-140 השאילתות המתויגות
של ההרצה הקודמת, מפאת זמן). זה חשף ממצא אמיתי:

### ממצא: "חלב" מתאים לחלב קוקוס בפרש מרקט

בפרש מרקט (תל אביב), החיפוש "חלב" נפתר ל-**"חלב קוקוס 400 מ"ל" (₪9.90)** ולא
לחלב פרה. הסיבה: "חלב קוקוס" מקבל ניקוד 0.850 — בדיוק אותו ניקוד כמו "חלב טרי
3% תנובה 1.5" (₪11.02) וכמה גרסאות חלב-פרה נוספות — כי הדמיון סופר רק "כמה
מהמילים בשם המוצר לא הוסברו על ידי השאילתה", ו"קוקוס" סופג בדיוק את אותו קנס
כמו "טרי" או "תנובה". בתוך חלון הצמידות (`NEAR_TIE=0.04`) המנוע בוחר את הזול
ביותר — וחלב קוקוס יוצא זול משאר הגרסאות שבטווח. שורה בודדת מתוך 5-6 רשתות בעיר
אחת, אבל היא בדיוק סוג הטעות שסל בקרה אמור לתפוס: שם קטגוריה (מוצר חלבי) לא
מוגן משם-קטגוריה אחר (תחליף צמחי) כשהמילה הראשונה חופפת. לא נבדק אם התקלה
חוזרת ברשתות/ערים אחרות — מומלץ להוסיף "קוקוס", "סויה", "שקדים" וכדומה
כמילות-ראש שדוחות התאמה ל"חלב" הרגיל, באותו האופן שבו `CATEGORY_WORDS` כבר
מטפל ב"גלידת מסקרפונה" מול "מסקרפונה".

### נגישות

נבדקו `#/`, `#/compare`, `#/history` בשתי ערכות הנושא (בהיר/כהה) ובשתי השפות
(עברית/RTL, אנגלית/LTR), בדסקטופ (1280 רוחב) ובנייד (375 רוחב, מצביע גס).
**אחרי סינון שלוש תקלות-מדידה (לא ממצאים אמיתיים, ראו למטה): אפס בעיות ניגודיות,
אפס יעדי-מגע מתחת ל-24px, אפס גלישה אופקית, אפס פקדים בלי שם נגיש.**

שלוש תקלות-מדידה שזוהו ונשללו במפורש (חשוב לתעד כדי שהרצות הבאות לא ייפלו
לאותה מלכודת):

1. **`requestAnimationFrame` קפוא בטאב לא-גלוי.** `AnimatedCurrency` (רכיב חדש,
   `src/components/AnimatedCurrency.jsx`) סופר מ-0 עד הסכום האמיתי דרך
   `useAnimatedNumber`, שרץ על `requestAnimationFrame`. הדפדפן הרץ ללא חלון גלוי
   (סשן לא-מוקצה) קופא את ה-rAF, כך שכל סכום כספי בעמודי ההשוואה וההיסטוריה
   הוצג כ"‏0 ‏₪" — כולל שורות שבפועל נמצאו (5/5, 4/5 וכו'). אימתתי מול Node שהסכום
   האמיתי לשופרסל בעגלת הבדיקה הוא ₪94.08 (5/5 פריטים), לא ₪0. **זה לא ניגוד-צבעים
   שבור וזו לא תקלה — זו אותה משפחת בעיה שהאזהרה על מעברי CSS מתארת, רק ב-JS.**
2. **רקע "כרית" על טאב פעיל.** בדיקת ניגודיות נאיבית שמטפסת ב-DOM עד שתמצא
   `background-color` פספסה `.tab-indicator` — sibling עם `position: absolute`
   שיושב מתחת לטקסט אך לא מעל בעץ ה-DOM. תיקנתי לבדוק את הרקע האמיתי דרך
   `document.elementsFromPoint`; הניגודיות האמיתית שם היא **8.28:1**, לא 1.1:1.
3. **כפתורי עריכה/מחיקה בהחלקה (`SwipeRow`, רכיב חדש) נחתכים ב-`overflow:hidden`**
   על `.swipe-row` וגלויים רק אחרי מחוות החלקה. פוסלו מהבדיקה כי משתמש שלא
   מחליק אף פעם לא רואה אותם.

### מסך שגיאה (נגרם על ידי נתוני בדיקה שלי, לא באג באתר)

בזמן הזרעת נתוני בדיקה ל-`localStorage` בעמוד ההיסטוריה נתקלתי בקריסה אמיתית:
`RangeError: Invalid time value` מתוך `formatDateTime`, שהעלתה את מסך "התצוגה
נעצרה" (`ErrorBoundary`). הסיבה: רשומת ההיסטוריה שיצרתי ידנית לא כללה
`completedAt`/`totalCost`/`itemCount` — שלושה שדות שה-reducer האמיתי
(`complete-purchase`) תמיד כותב יחד. **זו לא תקלה שנגישה מזרימת שימוש רגילה** —
אך שווה לציין פער הגנתי קטן: `loadData()` ב-`storage.js` מנקה בברירת מחדל
פריטי `activeList` (משלימה `code`/`unit`/`weight` חסרים) אך **לא עושה את אותו
דבר לרשומות `history`** — היא רק בודקת ש-`history` הוא מערך. אם אי-פעם רשומת
היסטוריה תיפגם (עריכה ידנית של `localStorage`, מיגרציית סכמה עתידית), העמוד
כולו יקרוס למסך שגיאה במקום להתעלם מהרשומה הפגומה. עדיפות נמוכה, לא נחוץ טיפול
מיידי — רק תיעוד.

### קונסולה

ללא שגיאות אמיתיות בשום עמוד/ערכת-נושא/שפה (מלבד אזהרות React Router
עתידיות סטנדרטיות ולוגי HMR של Vite, שתיהן צפויות בסביבת פיתוח ולא רלוונטיות
לפרודקשן).

### מה לא נבדק ולמה

- **לא הורץ שוב מבחן ה-140 שאילתות המתויגות** (7 רשתות × 20) מההרצה הקודמת —
  הוחלף בבדיקת סל הבקרה על 7 הרשתות בתל אביב בלבד, מפאת זמן. כדאי להריץ את
  המבחן המלא בהרצה הבאה כדי לוודא שדיוק ה-96.4% לא זז.
- **ממצא חלב-הקוקוס נבדק רק בפרש מרקט, תל אביב.** לא נבדק אם אותה התנגשות
  קיימת ברשתות/ערים אחרות שמוכרות תחליפי חלב צמחיים — שווה לבדוק בהרצה הבאה.
- **שרת הפיתוח הופעל ידנית דרך Bash, לא דרך `preview_start`** — הכלי סירב
  להפעיל שרת פיתוח מסשן לא-מוקצה ("nobody is present to approve"). זה גם הסיבה
  לתקלת ה-rAF שתועדה למעלה: הטאב נשאר `document.hidden === true` לכל אורך
  הבדיקה, ולכן אנימציות שמסתמכות על `requestAnimationFrame` לא רצות במציאות
  כפי שהן ירוצו אצל משתמש עם טאב פתוח וגלוי.

## 2026-08-23 · 12:20 — בסיס (הרצה ראשונה)

**מצב: תקין.** אין ממצא שדורש טיפול.

| מדד | ערך |
|---|---|
| בדיקות אתר | 79 עוברות |
| בדיקות בוט | 36 עוברות |
| בנייה | נקייה, ללא אזהרות |
| JS (gzip) | 77.12 kB |
| CSS (gzip) | 6.53 kB |
| סכמת קטלוג | 3 |
| גיל הקטלוג | 7.0 שעות |
| נגישות | 0 ממצאים ב-3 העמודים |
| זמן השוואה | 56 חיפושים ב-31ms |

### קטלוג

| עיר | רשתות | מוצרים | שקילים |
|---|---|---|---|
| תל אביב | 7 | 72,058 | 5.5% |
| ראשון לציון | 7 | 87,893 | 5.3% |
| חיפה | 6 | 56,169 | 5.0% |
| ירושלים | 7 | 68,587 | 5.3% |

כל הרשתות בטווח הצפוי (3-8%), **למעט דור אלון ב-0.0%** בשלוש ערים. זו אינה תקלה:
דור אלון היא רשת נוחות עם ~1,560 מוצרים ואינה מוכרת בתפזורת כלל. שווה לוודא שזה
נשאר כך — אם רשת אחרת תיפול ל-0%, זה כן יעיד על רגרסיה בחילוץ `BisWeighted`.

### סל בקרה (שופרסל, תל אביב)

```
חלב        -> חלב 3% מהדרין שקית 1 ל      ₪6.41
לחם אחיד   -> לחם אחיד 750 גרם            ₪7.36
ביצים      -> ביצים ארוזות XL 12יחידות    ₪15.48
שמן זית    -> שמן זית אורגני 750 מ"ל      ₪52.90
קוטג       -> קוטג מהדרין 9% 250 גרם      ₪6.40
אורז       -> אורז לסושי 1 ק"ג            ₪15.90
סוכר       -> סוכר לבן 1 ק"ג              ₪5.90
במבה       -> במבה מתוקה 30 גרם           ₪4.50
                                    סה"כ  ₪114.85
```

**דיוק התאמה נמדד על 140 חיפושים** (7 רשתות × 20 שאילתות מתויגות): **96.4% נכון**,
שגיאה אחת, ארבעה "לא נמצא" שכולם לגיטימיים בדור אלון.

### הערות שאינן ממצאים

- **כפתורי הסטפר 36×36 בדסקטופ.** לא תקלה: ה-CSS מקפיץ אותם ל-44px תחת
  `(pointer: coarse)`, ולכן במובייל אין ממצא. 36px עם עכבר עובר את רף WCAG 2.5.8 AA (24px).
- **`"ביצים"` בטיב טעם מתאים ל-`"ביצה קינדר גוי"`.** שני המוצרים מקבלים ניקוד
  **זהה בדיוק** (0.800), ולכן כלל "הזול מבין השווים" בוחר את הלא-נכון. אין סף או
  קנס שמפריד ביניהם, והתאמה ספציפית למוצר נובלטי בודד תזיק למנוע הכללי. הנתיב
  הראשי באתר הוא ברקוד מה-autocomplete, שם זה לא יכול לקרות.
- **`"אורז"` מתאים ל-`"אורז לסושי"`.** שניהם אורז; העדפה לאורז רגיל תדרוש ידע
  תחומי שאין למנוע. נמוך בעדיפות.

### שיפורים שבוצעו בהרצה זו

שלושה באגי התאמה תוקנו ונבדקו:

1. **`*מבצע*` לא נוקה.** `normalize` הסיר סוגריים ומרכאות אך לא כוכביות, ולכן
   `מבצע` מעולם לא הותאם למילת הרעש. **1,572 מוצרים בתל אביב לבדה** (2.2%) נשאו
   טוקן ראש מיותר שהוריד להם דיוק והפעיל עליהם קנס ראש שלא בצדק.
2. **מארז עם רווחים לא זוהה.** `"במבה מארז 20 * 15 גרם"` — `PACK_TOKEN` דרש
   `20*15` בלי רווחים, ולכן מארז של 20 הוצע למי שביקש שקית אחת:
   **₪29.90 במקום ₪4.50**, פי 7 באותה שורה.
3. **מנות שמכילות רכיב הותאמו לרכיב.** `"לאבנה עם שמן זית"` הותאם ל-`"שמן זית"`
   בדור אלון, שאין בה שמן זית כלל. נוספו ראשי-קטגוריה שנמצאו ע"י ספירת מילת
   הפתיחה של כל מוצר בקטלוג אמיתי.

`npm test` ירוק בשני הצדדים אחרי כל השלושה.
